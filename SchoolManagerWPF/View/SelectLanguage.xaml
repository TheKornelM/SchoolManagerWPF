<Window x:Class="SchoolManagerWPF.SelectLanguage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resx="clr-namespace:SchoolManagerModel.Resources;assembly=SchoolManagerModel"  
    mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">


    <Grid>
        <Label Content="{x:Static resx:UI.Language}" HorizontalAlignment="Left" Margin="236,68,0,0" VerticalAlignment="Top" FontSize="72"/>
        <ComboBox HorizontalAlignment="Center" Margin="0,179,0,0" VerticalAlignment="Top" Width="294"  Name="languageComboBox" SelectedIndex="{Binding LanguageId}">
            <ComboBoxItem Content="Magyar"/>
            <ComboBoxItem Content="English"/>
        </ComboBox>
        <Button Content="{x:Static resx:UI.Continue}" 
         HorizontalAlignment="Center" 
         Margin="0,295,0,0" 
         VerticalAlignment="Top" 
         Height="120" 
         Width="304" Name="continueButton" Command="{Binding ShowLoginCommand}"/>
    </Grid>
</Window>
